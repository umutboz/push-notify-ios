# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'push-notify' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!
  pod 'Firebase','8.5.0'
  pod 'Firebase/Analytics','8.5.0'
  pod 'Firebase/Messaging','8.5.0'
  
  # Pods for push-notify

  target 'push-notifyTests' do
    # Pods for testing
    pod 'Firebase/Analytics','8.5.0'
    pod 'Firebase/Messaging','8.5.0'
  end

end

target 'push-notifyExample' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  target 'push-notifyUITests' do
    # Pods for testing
  end

end
